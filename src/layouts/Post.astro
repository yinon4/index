---
import Layout, { type Props as BaseProps } from "./Base.astro";

export interface Props extends BaseProps {
  date?: string;
  tags?: string[];
  readingTime?: string;
}

const { date, tags, readingTime, ...baseProps } = Astro.props;
---

<Layout {...baseProps}>
  <slot />
</Layout>
