---
import { createLinks } from "../functions.astro";
import Post from "../layouts/Post.astro";

const subFolders = await Astro.glob("./*/*");
const folderFiles = await Astro.glob("./*");
const links = createLinks([...folderFiles, ...subFolders]);
---

<Post title="index of" description="index of" links={links} />
